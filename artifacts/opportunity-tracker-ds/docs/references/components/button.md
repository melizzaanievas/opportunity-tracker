# Button

**Source:** `artifacts/opportunity-tracker/src/components/ui/button.tsx`,
consumed by the login, add opportunity, and opportunity detail surfaces.

**Public behavior:** `default`, `destructive`, `outline`, `secondary`, `ghost`,
and `link` variants; `default`, `sm`, `lg`, and `icon` sizes; `asChild`
composition; native disabled and focus-visible behavior.

**Source treatment:** compact sans label, rounded control, subtle hover
elevation, and gold-accented emphasis within the atmospheric theme.

**Chunk:** 1 (pilot). **Status:** implemented.
