[providers]
providers = ['node']

[phases.setup]
nixPkgs = ['pnpm']

[phases.install]
cmds = ['pnpm install --no-frozen-lockfile']

[phases.build]
cmds = ['pnpm --filter ./artifacts/opportunity-tracker run build']

[start]
cmd = 'pnpm --filter ./artifacts/opportunity-tracker run preview'
